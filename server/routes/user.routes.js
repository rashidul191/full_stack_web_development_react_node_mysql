const express = require("express");
const router = express.Router();

/*
 * Route Prefix
 * /api/user_____
 */

// user routes
// router.use("/profile", require("./user/profile.route"));

module.exports = router;
